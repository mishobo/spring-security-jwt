version: "3.8"

services:
  mysqldb:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: 123456
    ports:
      - '3306:3306'
    volumes:
      - mysql-jwt-data:/var/lib/mysql


volumes:
  mysql-jwt-data:

